{
	"builders": [
		{
			"type": "digitalocean",
			"api_token": "{{user `api_token`}}",
			"region": "{{user `region`}}",
			"size": "512mb",
			"image": "ubuntu-14-04-x64"
		}
	],
	"provisioners": [
		{
			"type": "shell",
			"script": "provision.sh"
		}
	]
}
